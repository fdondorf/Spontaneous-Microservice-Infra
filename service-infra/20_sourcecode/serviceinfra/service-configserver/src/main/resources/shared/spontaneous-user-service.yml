spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      username: oauth-user
      password: password
      database: oauth-db
      
server:
  servlet:
    context-path: /user
  port: 8083