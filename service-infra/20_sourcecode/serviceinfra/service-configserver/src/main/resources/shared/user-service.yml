security:
  oauth2:
    client:
      clientId: spontaneous-client
      clientSecret: spontaneous-secret
      accessTokenUri: http://localhost:8081/uaa/spontaneous/secure/auth/token
      grant-type: client_credentials
      scope: tracking

server:
  servlet:
    context-path: /users
  port: 8084

feign:
  hystrix:
    enabled: true